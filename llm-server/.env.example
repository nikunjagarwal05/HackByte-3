# Google Gemini API key - required for the LLM functionality
GEMINI_API_KEY=YOUR_API_KEY

# Server configuration
PORT=3001              # Primary server port
TEST_PORT=3001         # Port used by test client (usually same as PORT)
API_HOST=localhost     # Server hostname